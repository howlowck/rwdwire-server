<?php

# Load the config when generator spark is loaded
$autoload['config'] = array('generator');

# Load the helper when the generator spark is loaded
$autoload['helper'] = array('generator');

# Load the library when the generator spark is loaded
$autoload['libraries'] = array('generator');

